<header>
  <nav class="navbar navbar-default navbar-static-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-left" href="/"><img src="/images/logo.png" alt="Monolist"></a>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to new_admins_shop_path do %>
            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
            店情報を追加
            <% end %>
         </li>
         <li class="nav-item">
           <%= link_to likes_like_path do %>
           <i class="far fa-star"></i>お気に入り一覧
           <% end %>
        </li>
        <li>
          <%= link_to articles_path do %>
            <i class="fas fa-video"></i> 動画一覧
          <% end %>
        </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <span class="gravatar">
                  <%= image_tag(current_user.image, options = { size: 30 }) if current_user.image.present? %>
                </span>
                <%= current_user.name%>
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li>
                 <%= link_to 'マイページ', user_path(current_user)%>
                </li>
                <li role="separator" class="divider"></li>
                <% if current_user.present? %>
                  <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
                <% elsif current_admin.present? %>
                  <li><%= link_to 'ログアウト(管理者)', destroy_admin_session_path, method: :delete %></li>
                <% end %>
              </ul>
            </li>
          <% else %>
            <li><%= link_to '新規登録', new_user_registration_path %></li>
            <li><%= link_to 'ログイン', new_user_session_path %></li>
            <li><%= link_to "管理者ログイン", new_admin_session_path %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
